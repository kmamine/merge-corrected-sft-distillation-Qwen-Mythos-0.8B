# Core stack
torch>=2.6.0
transformers>=4.50.0
accelerate>=1.0.0
datasets>=3.0.0
trl>=1.0.0              # SFTTrainer (distillation SFT)
lm-eval>=0.4.0         # EleutherAI lm-evaluation-harness (GSM8K / MMLU / ARC-Challenge)
peft>=0.13.0           # LoRA fallback
huggingface_hub>=0.25.0
mlflow>=3.0.0          # tracking client; match the running server (3.14.0 on :5000)
pyyaml>=6.0
numpy>=1.24
