"""Shared library for the aviation CPT + merge pipeline.

Modules:
    config — CPTConfig dataclass + YAML/CLI override resolution
    data   — Aviation_QA loading, token packing, replay, QA prompt formatting
    utils  — seeding, parameter counts, throughput, SQuAD EM/F1 metrics
"""
